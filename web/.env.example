# ============================================================
# D2trader Next.js 환경변수
# 이 파일을 .env.local로 복사 후 실제 값으로 교체하세요.
# cp .env.example .env.local
# ============================================================

# ── Python API 연결 (서버사이드 전용 — 브라우저에 노출되지 않음) ──
PYTHON_API_URL=http://localhost:8000
PYTHON_API_SECRET=your-internal-secret-here

# ── 배포 시 (Cloudflare Tunnel 경유) ──
# PYTHON_API_URL=https://api.d2trader.your-domain.com
